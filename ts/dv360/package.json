{
  "name": "launch_monitor",
  "type": "module",
  "devDependencies": {
    "@types/google-apps-script": "^1.0.56",
    "tsup": "^8.5.0",
    "typescript": "^4.9.3"
  },
  "scripts": {
    "build": "tsup",
    "push": "pnpm build && clasp push",
    "push:client": "pnpm --filter client push",
    "test": "pnpm --filter ts test dv360/src/tests/*_test.ts",
    "lint": "pnpm --filter ts lint dv360/src/**/*.ts"
  }
}
