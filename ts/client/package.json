{
  "name": "launch_monitor",
  "type": "module",
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.56",
    "@web/dev-server-esbuild": "^1.0.2",
    "@web/test-runner": "^0.18.2",
    "tsup": "^8.5.0",
    "typescript": "^4.9.3"
  },
  "scripts": {
    "build": "tsup",
    "push": "pnpm build && clasp push",
    "test": "echo 'no tests for client wrapper.'"
  }
}
