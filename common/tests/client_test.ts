import { FakeClient } from './helpers';

describe('Client args', () => {
  it('allows client construction when uninitialized', () => {});
  it('is accessible from the client when initialized', () => {});
});
