name: DV360 Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set bash to strict mode
        run: set -euox pipefail

      - name: Set Node.js 22.x
        uses: actions/setup-node@v3
        with:
          node-version: 22.x

      - name: Install yarn
        run: npm -g install yarn

      - name: Mock clasp
        run: >
          function clasp() {
            echo "LAST_CMD=$1" >> $GITHUB_ENV
          }

      - name: Build dv360 prod
        run: npx yarn --cwd ./dv360 build:prod

      - name: Build dv360 dev
        run: npx yarn --cwd ./dv360 build:dev

      - name: Build client
        run: npx yarn --cwd ./dv360 push:dev

      - name: Print errors
        run: find . -name "*.warn" -type f -exec cat {} \;

      - name: Fail on errors
        if: ${{ steps.build-dv360.outputs.error || steps.build-dev-dv360.outputs.error || env.LAST_CMD != 'push' }}
        run: exit 1
